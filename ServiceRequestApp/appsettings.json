{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=SAIFUL_ISLAM;Initial Catalog=ServiceRequestApp;Integrated Security=True;Connect Timeout=30;TrustServerCertificate=True"
  },
  //Data source is server name
  //Initial Catalog is database name
  //Integrated secrity is windows authentication
  //Connect Timeout is This specifies the time (in seconds) that the application will wait for a connection to the SQL Server to be established before timing out and generating an error.
  //Trust Server Certificate is This property is related to SSL/TLS encryption for the connection between your application and the SQL Server.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}